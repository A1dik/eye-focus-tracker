# 👁️ Eye Gaze Tracker - Focus Monitor

Приложение на Python которое следит за твоими глазами через веб-камеру. Если ты отводишь взгляд от монитора (смотришь влево, вправо, вниз или в телефон) - появляется громкое предупреждающее видео.

Отлично подходит для концентрации на работе/учёбе!

## 🎯 Возможности

- ✅ Отслеживание направления взгляда в реальном времени
- ✅ Детекция наклона головы (смотришь в телефон = предупреждение)
- ✅ Показ видео с усиленным звуком (x100) при отведении взгляда
- ✅ Строгий режим - срабатывает через 0.8 секунды
- ✅ Автоматическое закрытие видео когда возвращаешь взгляд
- ✅ Работает на чистом OpenCV (без сложных зависимостей)

## 📦 Установка

### 1. Клонируй репозиторий

```bash
git clone <url моего репо>
cd glaza
```

### 2. Установи зависимости

```bash
pip install -r requirements.txt
```

Требуется:
- Python 3.7+
- OpenCV
- NumPy
- Pygame (для звука)

### 3. Добавь своё видео-предупреждение

Положи файл `warning.mp4` в папку проекта. Это видео будет показываться когда ты отводишь взгляд.

**Важно:** Если у тебя видео в другом формате, переименуй его в `warning.mp4` или конвертируй:

```bash
ffmpeg -i your_video.mov warning.mp4
```

### 4. Извлеки и усиль звук (опционально ПРОВЕРЬ ЧТО ЕСТЬ ffmpeg!!!)

Если хочешь чтобы звук был громким (x100):

```bash
ffmpeg -i warning.mp4 -vn -af "volume=100" warning_audio.wav -y
```

Если `warning_audio.wav` не найден, видео будет без звука.

## 🚀 Запуск

```bash
python glaza.py
```

## ⌨️ Управление

- **ESC** - выход из приложения
- **SPACE** - закрыть предупреждение вручную

## 📊 Как работает

### Детекция взгляда

Приложение использует OpenCV Haar Cascades для:
1. Обнаружения лица
2. Обнаружения глаз
3. Определения положения зрачков
4. Вычисления направления взгляда

### Строгий режим

Предупреждение срабатывает если:
- 👀 Глаза отведены **влево/вправо** (зрачки не в центре)
- 📱 Голова наклонена **вниз** (смотришь в телефон)
- ❌ Лицо **не обнаружено** (отвернулся)

Порог срабатывания: **0.8 секунды**

### Статусы

- 🟢 **LOOKING CENTER** - всё ОК, работай дальше
- 🟠 **LOOKING LEFT/RIGHT** - смотришь в сторону
- 🔴 **HEAD DOWN** - наклонил голову (телефон/бумаги)
- 🔴 **NOT DETECTED** - лицо не видно

## ⚙️ Настройки

Открой `glaza.py` и измени параметры в классе `EyeTracker.__init__()`:

```python
# Порог срабатывания (секунды)
self.look_away_threshold = 0.8

# Размер истории для сглаживания
self.history_size = 3
```

### Чувствительность границ

В методе `detect_gaze()` можно изменить границы "центра":

```python
# Строгие границы (узкий центр)
if smooth_avg < 0.45:  # Смотрит влево
    return 'left', frame
elif smooth_avg > 0.55:  # Смотрит вправо
    return 'right', frame
else:
    return 'center', frame  # Центр
```

Уменьши значения (например, 0.47-0.53) для **ещё более строгого** режима.

### Проверка наклона головы

Измени порог положения лица:

```python
# Если лицо в нижней трети экрана
if face_relative_y > 0.65:  # Уменьши для строже (0.55)
    return 'down', frame
```

## 🎬 Рекомендуемый формат видео

- Формат: `.mp4` (H.264)
- Длительность: 2-5 секунд (зацикливается)
- Разрешение: любое (автоматически масштабируется)
- Звук: громкий, неприятный (чтобы мотивировать вернуться к работе)

### Идеи для видео

- Кричащий человек
- Мигающий красный экран
- Таймер обратного отсчёта
- Мотивационные фразы ("РАБОТАЙ!", "FOCUS!")
- Что угодно что заставит вернуться к работе

## 🔧 Устранение проблем

### Камера не работает

Попробуй изменить индекс камеры в `glaza.py`:

```python
self.webcam = cv2.VideoCapture(1)  # вместо 0
```

### Слишком частые срабатывания

Увеличь порог:

```python
self.look_away_threshold = 1.5  # вместо 0.8
```

Или расширь границы центра:

```python
if smooth_avg < 0.42:  # было 0.45
    return 'left', frame
elif smooth_avg > 0.58:  # было 0.55
    return 'right', frame
```

### Не срабатывает когда смотрю в сторону

Проверь освещение - должно быть хорошее освещение на лице.

Или сделай границы строже:

```python
if smooth_avg < 0.48:  # было 0.45
    return 'left', frame
elif smooth_avg > 0.52:  # было 0.55
    return 'right', frame
```

### Видео без звука

Убедись что создан файл `warning_audio.wav`:

```bash
ffmpeg -i warning.mp4 -vn -af "volume=100" warning_audio.wav -y
```

## 📝 Структура проекта

```
glaza/
├── glaza.py              # Основное приложение
├── requirements.txt      # Зависимости
├── README.md            # Документация
├── .gitignore           # Git ignore
├── warning.mp4          # Твоё видео (не в git)
└── warning_audio.wav    # Извлечённый звук (не в git)
```

## 🛠️ Технологии

- **OpenCV** - компьютерное зрение
- **NumPy** - математические операции
- **Pygame** - воспроизведение звука
- **Haar Cascades** - детекция лица и глаз

## 💡 Примеры использования

- 📚 **Учёба** - не отвлекайся на телефон
- 💼 **Работа** - фокус на экране
- 🎮 **Антипрокрастинация** - меньше залипаешь в соцсети
- 🧘 **Медитация на экран** - тренировка концентрации

## 🤝 Контрибуция

Pull requests приветствуются! Идеи для улучшения:

- [ ] GUI для настройки параметров
- [ ] Статистика за день/неделю
- [ ] Интеграция с Pomodoro таймером
- [ ] Мультимониторная поддержка
- [ ] Калибровка под пользователя
- [ ] Web-версия


## 📄 Лицензия

MIT License - используй свободно!
